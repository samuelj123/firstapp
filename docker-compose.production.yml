version: "3"
services:
  frontend:
    build: frontendtwr/.
    env_file:
      - .env
    networks:
      - myNetwork
  backend: 
    build: backendtwr/.
    env_file:
      - .env
    networks:
      - myNetwork
networks:
  myNetwork:
     external: true
