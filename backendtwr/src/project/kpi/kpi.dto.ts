type Category = ['Content Creation', 'Content Delivery', 'Marketing', 'Audience Relations']
export interface KpiDTO {
    kpi: string;
    type: string;
    pointperson: string;
    report: string;
    project: string;
}
